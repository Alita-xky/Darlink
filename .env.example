# OpenAI API 配置
# 从 https://platform.openai.com/api-keys 获取你的 API 密钥
OPENAI_API_KEY=sk-your-api-key-here

# 选择使用的模型（可选，默认值为 gpt-3.5-turbo）
# 推荐使用 gpt-4 以获得更好的效果（需要付费账户）
OPENAI_MODEL=gpt-3.5-turbo

# 自定义 OpenAI API 端点（可选）
# 如果使用代理或本地 OpenAI 兼容服务，设置这个值
# OPENAI_BASE_URL=https://api.openai.com/v1

# 如果不需要真实模型，可以留空 OPENAI_API_KEY，系统会自动降级到 Stub 模式
