# 拷贝为 .env.production 并填值。**.env.production 不要提交到 git**

# OpenAI（如果不填，model_service 会走 stub 模式）
OPENAI_API_KEY=
OPENAI_MODEL=gpt-3.5-turbo

# 站点配置
DARLINK_DOMAIN=dashboard.example.com

# 看板访问保护（可选，如果 nginx 加 basic auth）
# DASHBOARD_BASIC_AUTH_USER=admin
# DASHBOARD_BASIC_AUTH_PASS=change-me
